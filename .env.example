# API base URL (no trailing slash). Used for auth, budget, tax, chat.
VITE_API_URL=https://spendwise-api-sjk5.onrender.com
# Frontend origin (for CORS / OAuth redirects; set in backend)
# VITE_FRONTEND_URL=http://localhost:5173
